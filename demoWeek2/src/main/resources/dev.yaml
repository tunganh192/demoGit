server:
  port: 8081
  servlet:
    context-path: /demoWeek

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/demoweek2"
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  profiles:
    active=test: